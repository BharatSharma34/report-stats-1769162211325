name: Deploy
on: [push]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Sync
        run: |
          curl -X POST "https://voila-api.mmopro.in/sync-app" \
            -H "Content-Type: application/json" \
            -d '{"app": "report-stats-1769162211325", "repo": "https://github.com/BharatSharma34/report-stats-1769162211325.git", "github_token": "${{ secrets.PRIVATE_GITHUB_TOKEN }}"}'